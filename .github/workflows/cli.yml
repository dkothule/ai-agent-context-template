name: CLI Tests

on:
  push:
    branches: [main, 'feat/**', 'fix/**']
    paths:
      - 'packages/cli/**'
      - 'scripts/sync-templates.sh'
      - '.github/workflows/cli.yml'
  pull_request:
    paths:
      - 'packages/cli/**'
      - 'scripts/sync-templates.sh'
      - '.github/workflows/cli.yml'

jobs:
  test:
    name: Test — Node ${{ matrix.node }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        node: ['18', '20', '22', '24']

    steps:
      - uses: actions/checkout@v6

      - name: Set up Node.js ${{ matrix.node }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node }}
          cache: 'npm'
          cache-dependency-path: packages/cli/package-lock.json

      # Shell hooks must be LF on every platform. Windows runners check out with
      # core.autocrlf=true, so without .gitattributes these arrive as CRLF and
      # bash mis-parses them. i/lf w/lf is the expected state everywhere.
      - name: Report line endings of shipped hook scripts
        run: git ls-files --eol packages/cli/src/templates/*/hooks/*.sh

      - name: Install dependencies
        working-directory: packages/cli
        run: npm ci

      - name: Type check
        working-directory: packages/cli
        run: npm run typecheck

      - name: Build
        working-directory: packages/cli
        run: npm run build

      - name: Test
        working-directory: packages/cli
        run: npm test

  version-sync:
    name: Assert version sync
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '24'
      - name: Install dependencies
        working-directory: packages/cli
        run: npm ci
      - name: Build
        working-directory: packages/cli
        run: npm run build
      - name: Assert version sync
        working-directory: packages/cli
        run: node scripts/assert-version-sync.js
